# SELECT contains ?y which is not in WHERE clause.

SELECT REDUCED ?x ?y
{
   ?x <http://hello.com/exists/> 'apple' , 'orange' , 'grapefruit' ;
      <http://hello.com/existed/> 'peach', 'lemon' .
}
OFFSET 20
LIMIT 10
